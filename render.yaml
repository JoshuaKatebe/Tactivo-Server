services:
  - type: web
    name: tactivo-server
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PTS_ENABLED
        value: "false"
      - key: PORT
        value: 10000
